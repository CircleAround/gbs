machine:
  php:
    version: 5.6.5
  environment:
    CAKE_ENV: test
dependencies:
  pre:
    - sudo touch .env
  cache_directories:
    - ~/.composer/cache
database:
  override:
    - bin/cake migrations migrate
deployment:
  staging:
    branch: master
    heroku:
      appname: gbs-app
