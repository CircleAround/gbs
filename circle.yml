machine:
  php:
    version: 5.6.5
dependencies:
  pre:
    - sudo touch .env
deployment:
  production:
    branch: origin/77-circle-ci-push-to-heroku
    commands:
      - deployment_to_production.sh